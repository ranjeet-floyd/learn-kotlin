package com.kotlin.learn

fun main() {
    // Kotlin has powerful type interface.

    // Declare mutable variable and init it
    var a: String = "Initial"
    println(a)
    // Declare immutable variables
    val b: Int = 1
    val c = 3

    println(c)

    val d:Int
    val s = "True"
    if(s == "True"){
        d =1
    }
    else {
        d =2
    }

    println(d)
}